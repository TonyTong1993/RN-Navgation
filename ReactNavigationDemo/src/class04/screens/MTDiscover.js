'use strict';

import React, { Component } from 'react';

import {
  StyleSheet,
  View,
  Text,
} from 'react-native';

class MTDiscover extends Component {
	static navigationOptions = ({navigation})=>({
	title:'附近'
	})
  render() {
    return (
      <View />
    );
  }
}

const styles = StyleSheet.create({

});


export default MTDiscover;