'use strict';

import React, { Component } from 'react';

import {
  StyleSheet,
  View,
} from 'react-native';

class MTMerchant extends Component {
	static navigationOptions = ({navigation})=>({
	title:'逛一逛'
	})
  render() {
    return (
      <View />
    );
  }
}

const styles = StyleSheet.create({

});


export default MTMerchant;